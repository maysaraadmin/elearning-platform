apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: elearning
spec:
  selector:
    app: notification-service
  ports:
    - port: 8008
      targetPort: 8008
  type: ClusterIP
